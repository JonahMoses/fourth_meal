<div class="navbar navbar-inverse">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-inverse-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#"><%= @restaurant.title %></a>
  </div>
  <div class="navbar-collapse collapse navbar-inverse-collapse">
    <ul class="nav navbar-nav">
      <li><a href="/">Food Fight</a></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Menu<b class="caret"></b></a>
        <ul class="dropdown-menu" role="menu">
          <li><%= link_to "Full Menu", items_path %></a></li>
          <li class="divider"></li>
          <li class="dropdown-header">Categories</li>
          <li class="divider"></li>
          <li><a href="#">Another action</a></li>
          <li><a href="#">Something else here</a></li>
          <li><a href="#">Separated link</a></li>
          <li><a href="#">One more separated link</a></li>
        </ul>
      </li>
      <li><a href="#">Order History</a></li>
      <li><a href="#">Contact</a></li>
      <% if current_user && current_user.email %>
        <li><%= link_to "My profile", edit_user_path(current_user.id) %></li>
      <% end %>
      <% if current_user && current_user.admin? %>
        <li><%= link_to "Dashboard", dashboard_path %></li>
      <% end %>
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <% if @restaurant_order.first != nil %>
        <% if current_restaurant.id == @restaurant_order.first.restaurant_id %>
          <li><%= link_to "Items in cart: #{order_items_count}", @restaurant_order.first %></a></li>
        <% else %>
          <li><%= link_to "Items in cart: #{order_items_count}", "#" %></a></li>
        <% end %>
      <% end %>

      <%= render "users/user_nav" %>
    </ul>

  </div>
</div>
